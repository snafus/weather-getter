from setuptools import setup

setup(name='weather-getter',
      version='0.1',
      description='Similar weather getter api from openweathermap; adds to influxdb'
      url='http://github.com/',
      author='jw',
      author_email='jw@a.b',
      license='MIT',
      packages=['weather-getter'],
      zip_safe=False)

